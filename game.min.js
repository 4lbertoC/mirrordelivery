var requestAnimationFrame = window.requestAnimationFrame || window.mozRequestAnimationFrame || window.webkitRequestAnimationFrame || window.msRequestAnimationFrame;
window.requestAnimationFrame = requestAnimationFrame;
var a = document.getElementById("c").getContext("2d"),
	b = [100, 100],
	c = {};
window.addEventListener("keyup", function (d) {
	c[d.keyCode] = void 0
}, !1);
window.addEventListener("keydown", function (d) {
	c[d.keyCode] = Date.now()
}, !1);
(function g() {
	var e = b[0],
		f = b[1];
	c[37] && (b[0] = Math.max(0, e - 2));
	c[38] && (b[1] = Math.max(0, f - 2));
	c[39] && (b[0] = Math.min(500, e + 2));
	c[40] && (b[1] = Math.min(300, f + 2));
	a.clearRect(0, 0, 500, 300);
	a.setFillColor("red");
	a.fillRect(b[0], b[1], 5, 5);
	requestAnimationFrame(g)
})();